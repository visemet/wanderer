<configuration>

  <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
    <!-- encoders are assigned the type
         ch.qos.logback.classic.encoder.PatternLayoutEncoder by default -->
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <logger name="com.ning.http.client" level="INFO" />
  <logger name="edu.caltech.visemet.wanderer" level="DEBUG" />
  <logger name="edu.caltech.visemet.wanderer.extractors" level="INFO" />

  <appender name="FILE" class="ch.qos.logback.core.FileAppender">
    <file>wanderer.log</file>
    <append>true</append>
    <encoder>
      <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
    </encoder>
  </appender>

  <!-- Strictly speaking, the level attribute is not necessary since -->
  <!-- the level of the root level is set to DEBUG by default.       -->
  <root level="DEBUG">
    <appender-ref ref="STDOUT" />
  </root>

  <!-- <logger name="edu.caltech.visemet.wanderer" level="DEBUG" additivity="false">
    <appender-ref ref="FILE" />
  </logger> -->

</configuration>